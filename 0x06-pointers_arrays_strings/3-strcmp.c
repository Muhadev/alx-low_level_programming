#include "main.h"
/**
 * _strncmp - compare string values
 * s1: input value
 * @s2: input value
 * Return: s1[i] - s2[i]
 */
char *_strncpy(char *dest, const char *src, int n)
{
int i;
	i = 0;
while (s1[i] != '\0' && s2[i] != '\0')
{
if (s1[i] != s2[i])
{
return (s1[i] - s2[i]);
}
	i++;
}

return (0);
}
